# admin-ui

Minimal Next.js TypeScript admin UI for Fazoo (scaffold).

Run locally after installing dependencies from workspace root:

```bash
pnpm install
pnpm --filter admin-ui dev
```

This scaffold includes Tailwind config and a minimal app layout.
